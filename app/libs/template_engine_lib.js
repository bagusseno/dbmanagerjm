'use strict'

var fs = require('fs')
var views_path = '../views/'

exports.render = (file_name, data) => {

    fs.readFile(views_path + '')
    data.forEach(element => {
        
        var regex = /{[/w]}
    });
}